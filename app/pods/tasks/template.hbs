{{#if session.isAuthenticated}}
  {{outlet}}
  <h1>Tasks</h1>
  {{#each model as |task|}}
    <div class="well">
      <h4>{{task.title}}</h4>
      <p>{{task.description}}</p>
    </div>
  {{/each}}
{{else}}
  <div class="well">Please Login</div>
{{/if}}