<nav class="navbar navbar-inverse">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">EmberTasks</a>
    </div>
    <div id="navbar" class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
      	<li>{{#link-to 'tasks'}}My Tasks{{/link-to}}</li>
      	<li>{{#link-to 'tasks.new'}}New Task{{/link-to}}</li>
      </ul>
      <ul class="nav navbar-right">
        {{#if session.isAuthenticated}}
          <mediem style="color: white;">Logged in as {{session.currentUser.email}}</mediem>
          <button {{action "signOut"}} class="btn btn-success">Sign out</button>
        {{else}}
          <button {{action "signIn" "google"}} class="btn btn-success">Sign in with Google</button>
        {{/if}}
      </ul>
    </div><!--/.nav-collapse -->

  </div>
</nav>

<div class="container">
	<div class='starter-template'>
		<div class="col-md-12">
			{{outlet}}
		</div>
	</div>
</div><!-- /.container -->